apiVersion: v1
kind: Service
metadata:
  name: tickets-frontend
  namespace: arckium
  labels:
    app: tickets-frontend
spec:
  type: NodePort
  selector:
    app: tickets-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
      protocol: TCP
